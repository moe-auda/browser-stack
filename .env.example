# BrowserStack credentials — copy to .env and fill in your values
# Get these from: https://www.browserstack.com/accounts/settings
BROWSERSTACK_USERNAME=borisestrin1
BROWSERSTACK_ACCESS_KEY=ytznqgh8dGYzPdpFvTBn

# Optional: region for tests (usw, use, euw, apse)
SPEEDLAB_REGION=usw

# Optional: network throttling for mobile (4g, lte, 3g, unthrottled)
MOBILE_NETWORK=4g_normal

# Optional: network throttling for desktop (broadband_normal, broadband_fast, broadband_slow, un_throttled_desktop)
DESKTOP_NETWORK=broadband_normal
